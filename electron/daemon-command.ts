export const DAEMON_NODE_ARGS = ["--expose-gc", "--max-old-space-size=128"]
/** Match the worker fallback's heap budget for interactive, paged history reads. */
export const ON_DEMAND_CATALOG_NODE_ARGS = ["--expose-gc", "--max-old-space-size=768"]
