# Local Control agent issue ledger

Updated 2026-09-26. Tracks the supplied Cursor session feedback and the related
capture/API failures found while reproducing it. The [wayfinder](local-control-map.md)
owns delivery order; this file owns each complaint's disposition and closure check.
The [investigation](audits/2026-09-23/local-control-capture21/README.md) records the
source bundle's loss notices, repros, counterexamples and measurements. Missing
history and inferred causes are not treated as established facts.

**Status meanings:** “Fixed locally” means source and scoped regressions pass, not
that the running app has received them. “Partial” has specific work remaining.
“Open” needs implementation or evidence. “Cause corrected” rejects the reported
explanation but retains the user-facing failure and any diagnostic work.
All local fixes still pass through [installed acceptance](local-control-map.md#lc-23--preview-isolation-and-installed-browser-rollout).

## Discovery, targeting and API

| ID / reported problem | Current disposition | Remaining work and closure check |
| --- | --- | --- |
| A01 — Disconnected browsers give no way to connect. Agent issue 1. | **Partial; next operation fixed locally (September 26).** Public `control.connectBrowser(id)` exists and help includes the call. Discovery stays side-effect-free. Every `control.browsers()` row now carries `next`, the exact call its state allows or the user action it waits for: disconnected → `await control.connectBrowser(id)`; connected desk → `openTab({browser})`; connected Chromium → `openTab` with a URL, or `tabs` + `claimTab`; connecting → the same connect call, which waits; awaiting approval → the user must allow Mako in that browser's prompt; setup required → the user must add the extension; unavailable → the reason, and that connecting refuses. Outside a task the result's `next` says browser control exists only inside a Mako task. `test-computer-tools.ts` asserts every state. | Return truthful connection/capability state and the exact next operation. A fresh agent must discover, connect and select the saved browser through public API/help without private Settings calls, automatic substitution or a remote-debugging prompt. |
| A02 — Dev app is invisible / cannot screenshot itself. Agent issue 1. | **Cause corrected; result wording fixed locally (September 26).** The reported `--web` host PID owned zero native windows; this did not establish blanket self-process exclusion. The dev desk route passed screenshot/recording acceptance. An empty `control.windows(pid)` now says the process owns no native windows, that this is not a permission refusal, that web and headless hosts (including Mako's own `--web` desk) have none and to use the desk entry in `control.browsers()`, and that an app still launching may add windows. A PID with no process says so and points to `control.apps()`. `test-computer-tools.ts` asserts both against the parent process and an unused PID. | Clearly distinguish “no native windows”, unavailable permission/backend and an available desk/page route. Test real window-owning and headless hosts. Do not tell an agent permission is missing or an app is excluded without evidence. |
| A03 — Desk `openTab({url})` succeeds but opens the wrong page. Agent issue 2. | **Fixed locally.** Desk creation validates the URL before creating a window. Later navigation faults throw `TabNavigationError` containing the exact created target. | Installed regression must prove no unexpected window for an unsupported desk URL; after a later navigation failure, inspect the returned target without repeating creation. |
| A04 — Screenshot dimensions do not match the image an agent sees. Agent issue 3. | **Partial.** The claimed 1568→1024 mismatch was not reproduced as a service metadata bug; attachment display resizing is a possible cause. A separate scrollbar/content geometry mismatch was reproduced and fixed. | Verify actual returned file pixels, CSS viewport, DPR, clip and coordinate conversion across harness display resizing, scrollbars and scale. Show useful metadata without implying displayed attachment size is the coordinate system. Keep this distinct from R02's confirmed recording bug. |
| A05 — No discoverable element screenshot. Agent issue 4. | **Fixed locally.** Ref screenshots already existed; `tab.locator({role,name}).screenshot()` now resolves strictly and captures the element. Help includes examples. | A fresh agent captures one of several identical controls within the intended form without raw CDP or hand-calculated coordinates. Native element screenshots remain explicitly unsupported; do not silently widen to a desktop capture. |
| A06 — `get`, `select` and `locator` accept confusingly different queries; raw schema errors. Agent issue 5. | **Partial.** `select` accepts exact `role`/`name`. Shared input validation now gives bounded field corrections, examples and `not-dispatched` across common client/host boundaries; post-dispatch response failures retain unknown outcomes. Advanced `text`/`roles` remain documented. September 26 (local): wrong query shapes were probed across `get`, `select`, `locator` and `observe`. Selector faults now say role and name are both required and exact, that text search is `observe({query})` or `select({text})`, and that CSS selectors are not supported; `select({query})` points to `text`; `observe({text})` is refused (the host schema is strict) and its hint says `query` is the text search. Every public help example, in script and REPL syntax, is compiled and checked to call only methods the SDK defines; breaking one example's method name fails the test. | `test-control-input-errors.mjs` runs the probed calls through a real session (zero backend calls, `not-dispatched`, bounded messages) and the example check. The original rejected call was not retained in the source bundle, so it cannot be replayed; the probed shapes stand in for it. Remaining: a fresh agent handling an invalid request from public help alone. |
| A07 — Visible “Generate” text differs from its accessibility name. Agent issue 5. | **Fixed locally (September 26).** Page controls whose shown text differs from their accessible name now report both: `button "Create image" visibleText="Generate"`. It comes from the control's own text descendants (nested controls keep theirs), is omitted when equal to the name, and is not reported for text entry, whose shown text is its value. `observe({query})` and `select({text})` also search it. Matching stays exact on the accessible name. When `get` or a locator finds no exact name, a read-only follow-up search names the control whose visible text you asked for: `"Generate" is the visible text of button "Create image" (ref). Names match the accessible name exactly, so use locator({role:"button",name:"Create image"}); nothing was dispatched.` | Real Chrome: `aria-label` button, labelled link and `aria-labelledby` button report visibleText; equal-name controls and the textbox do not; `query:"generate"` finds the button. `test-browser-service.ts` (labelled control, query by either label, no repeat of equal text) and `test-control-input-errors.mjs` (locator miss makes one read-only follow-up and names the control; `get` names it; an unrelated miss keeps its candidates) pass. Installed fresh-agent use remains. |
| A08 — Help is fragmented and takes five calls to learn common operations. Agent issue 6. | **Partial.** No-argument help is consolidated. Executable scoped-edit/capture examples now pass on real Chromium with duplicate controls; `topic:"examples"` returns just the recipes. Connect, desk limitations and recording recovery are documented. | Test signatures, return shapes, recovery examples and target capabilities against the actual contract. A fresh agent must complete discovery → scoped edit → explicit verification → screenshot/recording without repeated help hunting. Later blind jobs passed but used 18 browser help/API calls and 16 logged native help/API calls plus initial help. Revised signed-package trials completed with two browser help calls and three native calls including one repeat; retain broader held-out learning-cost evaluation. [Review](local-control-cli-review.md). September 26: every public example now compiles and calls only defined SDK methods (see A06); return shapes and recovery examples are still checked only where a suite runs them. |
| A09 — Live app mutation escapes a supposed preview; stubbing triggered a real model request. Agent issue 7. | **Open.** The desk is a live Mako client, not a sandbox. Help says so, and since September 26 so does the desk's own `control.browsers()` guidance ("A live client of the real Mako app, not a sandbox: its clicks and edits change real conversations and settings"); documentation does not implement isolation. | Design and enforce a fixture/preview boundary at the host bridge. Test attempted provider calls and real app writes, including arbitrary evaluation/HMR behavior. A read-only label or renderer stub alone cannot satisfy the test. |
| A10 — Long calls yield confusing cell tickets and hit the program deadline. Agent issue 8. | **Fixed in the CLI source; installed rollout pending.** `exec --source-file` waits for completion and exposes no cell tickets. Deadlines remain; cancellation never replays input. `record stop --wait` collects session-owned finalization. | Actual CLI cancellation, broken-pipe uncertainty, repeated recording stop and cancelled finalization waiters pass. Repeat on the installed host; do not restore public continuation bookkeeping. |
| A11 — CLI composability, files, pipes and easier updates. Follow-up requirement. | **Implemented and locally tested; installation pending.** [CLI contract](local-control-cli.md) describes the shared engine, independent shell processes, exact leases, file output, strict handshake and failure behavior. | Retain separate-process CLI and real Linux job regressions. Native ARM64 CLI media, simultaneous programs and cancelled finalization waiters now pass [scoped acceptance](audits/2026-09-23/local-control-cli-media.md). Finish installed fresh-agent acceptance, sustained workloads/encoder failures and native x64 CLI verification. Replacing a CLI binary alone does not update an active engine. |
| A13 — Misspelled click options can fall through to left click. Found during input audit. | **Fixed locally.** `click(ref,{buton:"right"})` now refuses before taking the semantic left-click shortcut. Options derive from the pointer-operation schema. | Client and engine checks assert zero backend calls; preserve strict validation before any fast path. [Evidence](audits/2026-09-23/local-control-input-contract.md). |
| A12 — Human takeover can race agent input on the same target. September streaming review. | **Deferred by scope decision.** Prioritize low-latency agent use/viewing on local and remote targets. No takeover policy has been selected; it is not a blocker. | Reopen when interactive human control is scheduled; then settle in-flight/queued actions, held-input cleanup, disconnect and fresh observation on release. [Decision](local-control-streaming.md#human-takeover-deferred). |
| A14 — MCP remained the default despite the composable CLI. September 24 follow-up. | **Source migrated and scoped acceptance passed; installation pending.** Task supervisors now start desktop workers and supply the exact CLI/session at provider launch. Public Local Control MCP registration, adapter, export and launcher are removed. | Command-specific offline help and focused `api`; CLI regression, desktop crash cleanup and MCP absence checks pass. Fresh Codex/Claude/Cursor browser, Codex AppKit, real Linux ARM64 and package checks pass. Repeat installed Aside/native jobs and latest native x64 CLI acceptance. Fresh ACP runs remain unmeasured. Retain unrelated MCPs and private Cua protocol. [Migration](local-control-cli.md#cli-only-migration). |

| A15 — Result parsing/publication can report a completed command as not dispatched. | **Fixed locally.** Six CLI subprocess failure cases now return unknown outcome/exit 4 without replay; typed engine faults are preserved. | Included in candidate 7b0a2c623f935da0; retain malformed open/claim/exec/record and image publication checks through installed rollout. |
| A16 — Linux startup cancellation reports invalid input. | **Fixed locally.** SIGINT/SIGTERM return cancelled/exit 130; independent Linux checks verify process-group and private-file cleanup. | Included in the revised Mac candidate; repeat in the distributed Linux image and native x64 gate. CLI exit alone must not claim cleanup completed. |
| A17 — Dialog-triggering click waits about five seconds and cursor appears held. | **Fixed in revised candidate.** Paired Aside click fell from 5,016.6 to 313.2 ms; one release, explicit unknown interruption, one save. Modal focus-reset detachment is also fixed. | Installed acceptance follows the idle update; extension overlay update evidence remains separate. [Evidence](local-control-cli-modal.md). |
| A18 — Failed exec hides output from earlier completed steps. | **Fixed locally (September 26); installation pending.** Only the MCP REPL kept earlier blocks; script-mode runs (CLI `exec`, browser program server) dropped them. Every failed run now rejects with the blocks already emitted, carrying its cause's fault code/outcome so fault-only callers are unchanged. CLI `exec` prints them on stdout in the success shape (images saved to files, not inline), then the fault on stderr with its exit code and "Output emitted before the failure is on stdout." The same inline/spill budgets bound them; nothing is emitted twice. | `test-control-cli.mjs` (log + saved image before an invalid call, exit 2) and `test-browser-tools.ts` (log + image before the fault) pass; dropping the partial blocks fails each. Timeout/cancellation keep blocks through the same path; a dedicated cancellation case and installed CLI acceptance remain. |
| A19 — A refused call inside a browser program reports `protocol-error` / `unknown` while its message says nothing was dispatched. Found September 26. | **Fixed locally.** The host's `BrowserFault` crosses the program worker as a plain `ControlFault`; the browser program server recognized only `BrowserFault`. It now reads any fault's code/outcome. | `test-browser-tools.ts` asserts `invalid-request` / `not-dispatched`; reverting the fix fails it. |
| A20 — A native driver refusal (for example `input_busy`: "text input is already active") reports `native-driver-error` / `unknown` and blocks the window until it is observed again, although the driver sent nothing. Found September 26 while testing driver death. | **Fixed locally.** A structured refusal (`status: "refused"`, no delivery; the driver never delivers on a refusal) becomes `native-<code>` / `not-dispatched` on typed and raw calls, and no longer blocks the target. Refusals without that structure and partial deliveries ("delivered N of M") stay `unknown`. | `test-computer-tools.ts` asserts `native-input-busy` / `not-dispatched` on both paths and an immediate next raw call; reverting either the classification or the raw path's outcome check fails it. The real driver's refusal was captured from +mako.23 and retried during `test-native-driver-death-live.ts`. |
| A21 — A control program with a syntax error reports `unknown`, as if some of it might have run. Reported September 26. | **Fixed locally.** A program that fails to compile never ran. REPL cells (MCP `js`) and scripts (CLI `exec`, browser programs) now fail with `syntax-error` / `not-dispatched`, the parser's message and line, and "No statement ran. Fix the source and run it again." instead of the uncertainty recovery. A script that ends early says it ends before every bracket, string or statement is closed. A `SyntaxError` thrown while running (for example `JSON.parse`) keeps `unknown`, because earlier statements ran. | `test-control-repl.ts` covers REPL and script compile errors, REPL-only illegal `return`, line and unclosed-input messages and runtime `SyntaxError`s; `test-control-cli.mjs` covers the CLI. Included in candidate `26b4f06bc693a102`. |

Workstream owners: [LC-22 API](local-control-map.md#lc-22--agent-facing-contract-and-discovery),
[LC-20 CLI](local-control-map.md#lc-20--shared-engine-and-composable-cli),
[LC-23 isolation/installation](local-control-map.md#lc-23--preview-isolation-and-installed-browser-rollout).

## Capture, recording and preview

| ID / reported problem | Current disposition | Remaining work and closure check |
| --- | --- | --- |
| R01 — First small frame makes the entire video 480×300 and blurry. | **Fixed locally for output sizing; source fidelity remains partial.** Encoding chooses fixed dimensions from the largest valid source frame, not the first. | Verify decoded output under initial downscale and later size changes, and explain when source detail was unavailable. Upscaling a small frame cannot recreate lost detail; do not equate low bitrate alone with bad quality. |
| R02 — Timeline reports 1600×1000 for an actual 480×300 JPEG. | **Fixed locally.** Timeline v2 records actual encoded pixels separately from viewport dimensions; receipts include output dimensions. | Independently decode frames and compare metadata/output, including mixed source sizes, DPR and native cursor mapping. Preserve this regression in package acceptance. |
| R03 — Interleaved clipped screenshots corrupt the screencast. | **Fixed locally.** Preview/recording share one stream. Ordinary viewport crops avoid compositor resizing; disruptive full-page/raw clips pause and restore full-view capture before resuming. Pausing alone failed and was rejected. | Installed real-browser tests must retain correct frame geometry through region/ref/full-page/raw screenshots, cancellation and target closure. Verify neither consumer stops the other's stream. |
| R04 — Retina detail, DPR=1 and a 1600px cap make icons/text soft; expose scale/fps. | **Partial.** `maxSide` and fps are explicit; pixel provenance is accurate. There is no general recording DPR/scale contract yet. | Define source density versus output cap, expose supported options and report limitations. Test text/icon readability at 1×/2× and small/background windows. Do not force viewport/DPR changes on a regular user tab merely to make the recording appear sharper. |
| R05 — Why cap capture at 30 instead of 60 fps? | **Source-rate plumbing implemented; sustained acceptance partial.** Browser default/max is 60. Current native capabilities carry requested fps: Mac/X11 advertise 60, GNOME 5. The earlier +19 Mac 60-second trial reached 57.35 distinct fps; the sustained browser run reached 52.02. | Prove sustained actual frames and click-to-visible latency per installed target. Replace GNOME's slow source, not merely encode duplicate frames at 60. [Current evidence](local-control-map.md#lc-21--responsive-capture-recordings-and-cursor). |
| R06 — Codex icon is an indistinct blob inside the recording's cursor/ring. | **Partial.** The reporting agent enlarged the app glyph from 20px to 24px. That is a separate app UI edit, not evidence that Local Control cursor design is complete. | Inspect compressed output at ordinary viewing size for cursor/glyph legibility, click indication and coordinate accuracy. Distinguish an app's loading glyph from the agent cursor. Cover scrolling, drag and native gestures; semantic actions without pointer dispatch must not invent motion. |
| R07 — Preview feels laggy / native panel shows only a still. | **Fixed locally for polling/ownership and decode starvation; installed acceptance open.** A bounded viewer decoder replaces repeatedly cancelled async-image loads; compositor tests reach ~59–60 distinct fps from 1080p source pixels. Shared streams and hidden-consumer release remain. [Evidence](local-control-preview-evidence.md). | Repeat on installed shared-host/Aside and remote paths, full-size/high-DPR viewers, visibility/retargeting and sustained load. Local two-viewer + recording input/fidelity/cleanup passed. Native 60 fps constraints remain requests, not measured delivery. |
| R08 — Capture does unnecessary work and may slow control. | **Partial.** Bounded JPEGs bypass repeated decode/resize/re-encode; one pending frame bounds recording work; ACKs are independent of disk/UI. Text-only reads explicitly avoid screenshots. | Measure CPU, memory, bytes and input latency. Dense preview JSON can reach ~555 KB/frame and ~33 MB/s at 60 fps. Compare transport alternatives with unchanged text fidelity and no unwanted image work for semantic reads. |
| R09 — Background/no-frame recording can appear to start successfully. | **Fixed locally for truthful startup; coverage open.** Start now waits for encoder admission of the first frame, and rejects encoder failure even if a JPEG arrived. Worker loading precedes capture to avoid an initial frozen frame. No-frame timeout still refuses without activating the tab. | Clear recovery must preserve target/foreground and avoid silent activation, movement or replacement. Test regular-profile background, occluded, minimized and inactive cases independently; disclose unsupported capture modes before promising a recording. |
| R10 — Long runs, restart/cancellation and dependency failures can lose media. | **Partial.** Final-frame flush, retained interruption receipts, bounded finalization, bundled Mac encoders and ARM64/Intel cloud cleanup have evidence. | Exercise sustained size/duration limits, disk pressure, encoder failure, resize, crashes/reloads and abrupt teardown on packaged targets. Retain playable partial media where possible and explicit failure otherwise. Include matching action/cursor timelines; audio recording remains unsupported. |
| R11 — Host fps is being used where displayed smoothness and interaction latency matter. September streaming review. | **Partial: local compositor evidence added.** Independent pixel markers measure distinct frames, gaps and click/type/scroll-to-visible; exact source-state and full decoded-pixel checks pass. [Measured results and scope](local-control-preview-evidence.md). | Measure installed/remote and full-size viewers, stage costs, independent second-viewer timing and instrumentation overhead; local offscreen composition is not physical display or network latency. [Plan](local-control-streaming.md#1-measure-the-viewer-before-selecting-a-transport). |
| R12 — Full-frame processing and separate viewers may waste capture/encoding work. | **Partial.** Two production viewers plus recording start one capture; each closes independently, with identical decoded pixels. Negotiated lossless compression reduced shared-socket response bodies by 32.86%; Electron IPC still carries expanded preview JSON. A later CPU-instrumented run was slower, so sustained resource/tail acceptance remains open. Damage-aware processing, remote transport and compatible encoder sharing remain unproven. September 26 (local): viewers no longer process full frames; each decodes and paints only its displayed device pixels (viewer renderer CPU per frame −22%, whole viewer −8%, fps unchanged; [A/B](local-control-media-fixes.md#displayed-size-viewer-decoding-local-september-26)). | Compare WebRTC/local candidates against JPEG baseline; measure idle/active CPU, bytes and one/two-viewer plus recording costs. Preserve text fidelity, source scope and independent recording quality. [Experiments](local-control-streaming.md#2-compare-transport-and-capture-changes-separately). |
| R13 — Quiet/hidden sources can be confused with a broken connection. | **Open acceptance gap, not a reproduced Mako reconnect bug.** No-first-frame refusal exists; distinct source/viewer/transport lifecycle evidence is incomplete. | Test idle, hide/show, source loss, sleep/wake and transport failure separately. Recover the failed component without disrupting healthy viewers/recordings or restoring stale input leases. [Recovery](local-control-streaming.md#4-recover-only-the-failed-component). |
| R14 — Audio delivery and audio recording lack an explicit capability/acceptance plan. | **Deferred capability expansion.** It is not required for current low-latency video work. Neither video capture nor owned-tab audio cleanup establishes audio streaming/recording. | Specify output source, mute/opt-in, A/V sync and cleanup separately for preview and files; do not implicitly capture microphone/unrelated-app audio. [Scope](local-control-streaming.md#audio-scope). |
| R15 — Native screenshot options silently ignored. Found during CLI acceptance. | **Fixed locally.** `maxSide:320` previously returned 640×420; shared native capture now returns 320×210, honors JPEG options and maps resized pixel input to the driver frame. Default bytes bypass reencoding. | Real GTK files/JPEG and shared-engine coordinate/stale-view checks pass. Preserve installed Mac/Linux tests; background X11 pointer input remains explicitly unsupported. [Evidence](audits/2026-09-23/local-control-cli-media.md). |


| R16 — Native fps was fixed and Linux capture stalled on pipe writes. | **Fixed in the +19 candidate; broader acceptance open.** Source fps now reaches ScreenCaptureKit/X11, and GNOME declares its real 5 fps ceiling. The Linux writer slept 5 ms every time its pipe filled; the 60 fps request retained only 33 source frames in 2.37 seconds. Writability polling raises the covered 640×420 source to 56.70 fps; encoded output fps is reported separately. | Measure retained source frames separately from CFR output, plus exact pixels, interruption cleanup and sustained 1080p CPU/memory. [Backend plan](local-control-capture-backends.md). |
| R17 — First-operation Mac recording crashes in ScreenCaptureKit. | **Reproduced.** The +18 candidate aborted in `SCContentFilter` / `SLSGetDisplaysWithRect`; a prior observation avoided that crash. +19 explicitly initializes the CoreGraphics display connection and checks window geometry before constructing the filter. First-operation capture passed at 1920×1080 with 577 distinct frames in 10.03 s and unchanged foreground samples. | Recording must work as the first operation with no extra agent observation, activation or capture-scope change. Retain first-use and repeated-start checks. |
| R18 — Sustained browser recording fills the source-JPEG storage cap. | **Fixed locally; sustained performance/rollout open.** Continuous encoding replaces JPEG files with bounded timestamped source history. One- and ten-minute repeated-pixel tests accepted 6.16/9.82 GB of source bytes with no source files. The final timestamped-queue Aside run completed 67 s, but preview/video motion reached only 56.53/56.82 distinct fps. Real encoder pause/resume preserves intermediate states; byte overflow interrupts below 32 MiB. September 25: a matched A/B found a 2.03 s video freeze once encoder lag passed its two-second history; the scheduler now skips evenly instead, covered by a throttled-encoder test; installed in `6ac3f4fbd690b74d`, whose two-minute installed runs recorded 58.17/58.70 fps with 83/133 ms longest holds. Sustained 60 fps under heavy contention remains open. | Preserve the original failure and complete sustained distinct-frame, input-latency and installed acceptance. The encoded-output storage limit remains; it was not raised. [Implementation and evidence](local-control-media-investigation.md#implementation-and-acceptance--september-24). |
| R19 — Native recording re-encodes an already encoded video even with `cursor:false`. | **Fixed locally for no-transform output.** Verified H.264 with matching fps and acceptable size is retained directly; the byte hash is unchanged. | Installed/native-backend acceptance and source-side cursor composition remain. Cursor/resize transforms still require postprocessing. [Evidence](local-control-media-investigation.md#implementation-and-acceptance--september-24). |
| R20 — Preview timestamp describes publication, not capture. | **Timestamp and binary delivery implemented and locally checked; overload reliability/rollout open.** Source `capturedAt` survives beside separate `publishedAt`; delayed-frame regression passes. | Binary packets retain source/publication times. Keep receipt/presentation clock attribution and independent marker timing as the latency check. [Delivery evidence](local-control-preview-binary.md). [Investigation](local-control-media-investigation.md#implementation-and-acceptance--september-24). |
| R21 — Encoder crash leaves ordinary MP4 unreadable. | **Browser encoder/host-death recovery fixed locally; broader faults open.** Capture-time fragmented MP4 retains a decoded prefix after encoder or host death. A killed host leaves an open-ended journal, never a fabricated completed receipt. Stalled pipes fail explicitly and reap the child. | Real ENOSPC/partial filesystem writes, power loss, broader players, installed rollout and post-restart handle recovery remain. Native transformed recordings still use ordinary MP4 postprocessing. [Reproducer and results](local-control-media-investigation.md#implementation-and-acceptance--september-24). |
| R22 — Under heavy load the preview silently drops frames. September 26 decision. | **Shown locally (user decision: accept a lower rate under heavy load and show it).** The host counts source-clock 60 fps slots with a frame; each viewer compares its painted frames to that and shows "N of M fps" below 90% of full rate, clearing at 95% or a second after the last paint. Ordinary audit: 59.41 fps, never labelled. Load ~41: 40.87 fps, labelled in 4 of 27 samples; Chromium itself produced 41–51 fps there, which the label cannot count. [Evidence](local-control-media-fixes.md#preview-shown-rate-under-load-local-september-26). | Install candidate `26b4f06bc693a102` and repeat an ordinary and a loaded audit on the installed app. The 55-fps gate applies to ordinary load. |


Owner: [LC-21 capture](local-control-map.md#lc-21--responsive-capture-recordings-and-cursor).
Packaging and final installed proof also depend on LC-26 and LC-23.

## Other reported blockers and inherited correctness work

| ID / report | Current disposition | Closure check / owner |
| --- | --- | --- |
| B01 — Full-tree lint flags `message: object` in the cloud worker. | **Fixed locally.** Final full lint passed with zero errors and five existing React/TanStack warnings. | Preserve full-tree checks; distinguish an unrelated concurrent failure from one introduced here. See the capture audit's `verification/` logs. |
| B02 — Raw-call uncertainty and broad invalidation from the earlier API review. | **Target recovery implemented and fault-tested locally.** Browser/raw native failures preserve unknown outcomes; exact-target reads recover, unrelated refs survive, driver reconnect and failed captures cannot clear uncertainty. Late monitoring failure, cancelled waits/programs, overlapping observations and waiting dialogs have regressions. | Browser-wide cookies, raw CDP restrictions, attach/detach ownership, in-flight profile locks and lost-reply recovery now pass local regressions in the [shared-engine audit](audits/2026-09-23/local-control-session-cli.md). Real driver/daemon death during native input is implemented and passes fixture and real-driver tests ([LC-08/14](local-control-map.md#lc-08--lc-14--uncertainty-ownership-and-independent-targets)). Remaining: installed acceptance and installed replacement. [Recovery audit](audits/2026-09-23/local-control-recovery.md); [LC-08/14](local-control-map.md#lc-08--lc-14--uncertainty-ownership-and-independent-targets). |
| B03 — “Passed locally” repeatedly confused with “available in the installed app.” | **Scoped installed runtime acceptance passes.** Build `3c1d563e25a9bd78` passes installed Mac/Aside and Codex MCP recovery checks. Earlier failed installers and no-frame capture findings remain historical evidence. | Sustained media and other provider/platform cells remain separate. New media investigation changes tests/docs only; it has not changed installed recording. [Installed receipt](local-control-mcp-deployment.json), [LC-23](local-control-map.md#lc-23--preview-isolation-and-installed-browser-rollout). |
| B04 — The native driver reports `effect: confirmed` for text it did not type. Found September 26 while testing driver death. | **Fixed in driver +mako.24 (installed for new driver launches).** Upstream `typed_progress` counted an insert as complete when the field's value *contained* the text. A view that accepts `AXSelectedText` without acting then "confirmed" any text already present: with +mako.23, "q" into "q" and "qw" into "qw" reported `route: accessibility`, `confirmed`, value unchanged. +24 requires the value to change as an insertion would (the text placed at one position, replacing at most the selection) or to gain an occurrence of the text. An unchanged value confirms only when the selection before typing was that same text. Otherwise the result is unchanged and the driver falls back to key events. | Driver unit tests cover repeated text, mid-string insertion, selection replacement, app completion and a polled no-op. `test-native-driver-death-live.ts` types "y" into a key-fixture field holding "zzyy" and requires "zzyyy"; this passed six of six runs on +24, including one on the installed driver. |
| B05 — A native daemon killed between a key's down and up events leaves that key pressed in the app. Found September 26 while testing driver death (two of five random kills). | **Fixed in driver +mako.24 on macOS (installed for new driver launches).** The daemon journals each key or mouse-button press to `<socket>.held-input` before posting it, and clears the entry after posting the release. The replacement daemon on the same socket posts the missing releases before it serves. Releases go only to the same process, identified by pid and start time; a reused pid gets nothing. On SIGTERM, which is how Mako stops the daemon, and on a clean shutdown, the daemon releases held input itself, deletes the journal and posts no further presses. Mako's startup sweep removes journals of dead hosts. | `test-native-driver-death-live.ts` has the fixture signal the daemon from inside a key-down handler. +mako.23 fails it (`held: [0]`, nothing releases the key). +24 passes six of six: after SIGKILL the next event for that key is a key up from the replacement daemon, and after SIGTERM it is a key up from the terminated daemon. The journal is gone after the host stops the daemon. Not covered: Linux and Windows, the standalone `mcp` mode without a daemon, and a daemon killed when the host's restart budget is spent (its journal is swept, and the key stays held). |

## How to close an issue

Attach the reproducer, changed code/contract and independent outcome check. State
which target/build was tested and whether deployment happened. Preserve rejected
explanations and failed candidates. Link the original complaint to its check;
“better help”, “60 fps output” or a passing unit test alone does not close an
end-to-end usability or fidelity complaint. Reopen an entry if a real agent still
needs a workaround that its closure check says should be unnecessary.


## September 24 acceptance findings

- **R09:** source-host Aside live capture now shares temporary focus emulation
  with input. Capture/screenshot/transport-loss restoration passes. CDP clicks
  can retain page `hasFocus()` while hidden; physical foreground and DOM focus
  are reported separately. No tab activation or forced blur fallback.
- **R10:** fixed the reproduced 512 MiB PNG staging failure with bounded encoder
  streaming. A 37.73-second two-viewer recording completes with the minimal
  bundled encoder. Static duration and encoder failure/reaping have regressions.
- **R08/R12:** 1080p two-viewer delivery reaches 58.03 distinct fps for 30 seconds,
  but expanded IPC remains 17.67 MB/s. The 60-second 1440p run reaches only
  34.00 fps. High-resolution efficiency remains open; preserve the slower run.
- **A05/A08/A10:** fresh agents pass installed CLI/MCP scoped editing, exact
  values, stdin/files and one-element capture. CLI `shot --format png|jpeg` now
  makes encoding explicit; filename extensions alone never selected it.
- **Native keyboard:** source high-level Return now completes the verified
  Terminal job. Explicitly off-screen siblings no longer over-restrict the
  admission hint. Recording's live AX dialog still trips the driver's ambiguity
  guard; title/size guesses do not remove it.

[Evidence and remaining acceptance](local-control-preview-evidence.md#september-24-capture-ownership-and-long-recording).
