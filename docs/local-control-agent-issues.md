# Local Control agent issue ledger

Updated 2026-09-24. Tracks the supplied Cursor session feedback and the related
capture/API failures found while reproducing it. The [wayfinder](local-control-map.md)
owns delivery order; this file owns each complaint's disposition and closure check.
The [investigation](audits/2026-09-23/local-control-capture21/README.md) records the
source bundle's loss notices, repros, counterexamples and measurements. Missing
history and inferred causes are not treated as established facts.

**Status meanings:** “Fixed locally” means source and scoped regressions pass, not
that the running app has received them. “Partial” has specific work remaining.
“Open” needs implementation or evidence. “Cause corrected” rejects the reported
explanation but retains the user-facing failure and any diagnostic work.
All local fixes still pass through [installed acceptance](local-control-map.md#lc-23--preview-isolation-and-installed-browser-rollout).

## Discovery, targeting and API

| ID / reported problem | Current disposition | Remaining work and closure check |
| --- | --- | --- |
| A01 — Disconnected browsers give no way to connect. Agent issue 1. | **Partial.** Public `control.connectBrowser(id)` now exists and help includes the call. Discovery is intentionally side-effect-free. | Return truthful connection/capability state and the exact next operation. A fresh agent must discover, connect and select the saved browser through public API/help without private Settings calls, automatic substitution or a remote-debugging prompt. |
| A02 — Dev app is invisible / cannot screenshot itself. Agent issue 1. | **Cause corrected.** The reported `--web` host PID owned zero native windows; this did not establish blanket self-process exclusion. The dev desk route passed screenshot/recording acceptance. | Clearly distinguish “no native windows”, unavailable permission/backend and an available desk/page route. Test real window-owning and headless hosts. Do not tell an agent permission is missing or an app is excluded without evidence. |
| A03 — Desk `openTab({url})` succeeds but opens the wrong page. Agent issue 2. | **Fixed locally.** Desk creation validates the URL before creating a window. Later navigation faults throw `TabNavigationError` containing the exact created target. | Installed regression must prove no unexpected window for an unsupported desk URL; after a later navigation failure, inspect the returned target without repeating creation. |
| A04 — Screenshot dimensions do not match the image an agent sees. Agent issue 3. | **Partial.** The claimed 1568→1024 mismatch was not reproduced as a service metadata bug; attachment display resizing is a possible cause. A separate scrollbar/content geometry mismatch was reproduced and fixed. | Verify actual returned file pixels, CSS viewport, DPR, clip and coordinate conversion across harness display resizing, scrollbars and scale. Show useful metadata without implying displayed attachment size is the coordinate system. Keep this distinct from R02's confirmed recording bug. |
| A05 — No discoverable element screenshot. Agent issue 4. | **Fixed locally.** Ref screenshots already existed; `tab.locator({role,name}).screenshot()` now resolves strictly and captures the element. Help includes examples. | A fresh agent captures one of several identical controls within the intended form without raw CDP or hand-calculated coordinates. Native element screenshots remain explicitly unsupported; do not silently widen to a desktop capture. |
| A06 — `get`, `select` and `locator` accept confusingly different queries; raw schema errors. Agent issue 5. | **Partial.** `select` accepts exact `role`/`name`. Shared input validation now gives bounded field corrections, examples and `not-dispatched` across common client/host boundaries; post-dispatch response failures retain unknown outcomes. Advanced `text`/`roles` remain documented. | Validate all public examples and provide a short actionable invalid-argument message with valid keys/example. Test the recorded rejected call. Adding one accepted field does not close general error usability. |
| A07 — Visible “Generate” text differs from its accessibility name. Agent issue 5. | **Partial.** Help explains accessibility-name matching; the two labels are not interchangeable. | Evaluate exposing visible text alongside accessible name and useful ambiguity candidates. Prove the agent can identify the intended button without silently broadening exact-name matching or creating another fuzzy selector. |
| A08 — Help is fragmented and takes five calls to learn common operations. Agent issue 6. | **Partial.** No-argument help is consolidated. Executable scoped-edit/capture examples now pass on real Chromium with duplicate controls; `topic:"examples"` returns just the recipes. Connect, desk limitations and recording recovery are documented. | Test signatures, return shapes, recovery examples and target capabilities against the actual contract. A fresh agent must complete discovery → scoped edit → explicit verification → screenshot/recording without repeated help hunting. |
| A09 — Live app mutation escapes a supposed preview; stubbing triggered a real model request. Agent issue 7. | **Open.** The desk is a live Mako client, not a sandbox. Help now says so; documentation does not implement isolation. | Design and enforce a fixture/preview boundary at the host bridge. Test attempted provider calls and real app writes, including arbitrary evaluation/HMR behavior. A read-only label or renderer stub alone cannot satisfy the test. |
| A10 — Long calls yield confusing cell tickets and hit the program deadline. Agent issue 8. | **Partial.** Exclusive start/resume shapes, numeric copyable cells and pre-dispatch validation cover the recorded `cell:"label"`, `source + cell`, and `code` mistakes. | Fresh-agent tests must cover yielded work, repeated status, timeout/cancel and retained recordings. CLI must collect continuations internally and offer a clear wait operation. Do not remove deadlines or replay uncertain input to hide the problem. |
| A11 — CLI composability, files, pipes and easier updates. Follow-up requirement. | **Implemented and locally tested; installation pending.** [CLI contract](local-control-cli.md) describes the shared engine, independent shell processes, exact leases, file output, strict handshake and failure behavior. | Retain the MCP/CLI and real Linux job regressions. Native ARM64 CLI media, simultaneous programs and cancelled finalization waiters now pass [scoped acceptance](audits/2026-09-23/local-control-cli-media.md). Finish installed fresh-agent acceptance, sustained workloads/encoder failures and native x64 CLI verification. Replacing a CLI binary alone does not update an active engine. |
| A13 — Misspelled click options can fall through to left click. Found during input audit. | **Fixed locally.** `click(ref,{buton:"right"})` now refuses before taking the semantic left-click shortcut. Options derive from the pointer-operation schema. | Client and MCP checks assert zero backend calls; preserve strict validation before any fast path. [Evidence](audits/2026-09-23/local-control-input-contract.md). |
| A12 — Human takeover can race agent input on the same target. September streaming review. | **Deferred by scope decision.** Prioritize low-latency agent use/viewing on local and remote targets. No takeover policy has been selected; it is not a blocker. | Reopen when interactive human control is scheduled; then settle in-flight/queued actions, held-input cleanup, disconnect and fresh observation on release. [Decision](local-control-streaming.md#human-takeover-deferred). |

Workstream owners: [LC-22 API](local-control-map.md#lc-22--agent-facing-contract-and-discovery),
[LC-20 CLI](local-control-map.md#lc-20--shared-engine-and-composable-cli),
[LC-23 isolation/installation](local-control-map.md#lc-23--preview-isolation-and-installed-browser-rollout).

## Capture, recording and preview

| ID / reported problem | Current disposition | Remaining work and closure check |
| --- | --- | --- |
| R01 — First small frame makes the entire video 480×300 and blurry. | **Fixed locally for output sizing; source fidelity remains partial.** Encoding chooses fixed dimensions from the largest valid source frame, not the first. | Verify decoded output under initial downscale and later size changes, and explain when source detail was unavailable. Upscaling a small frame cannot recreate lost detail; do not equate low bitrate alone with bad quality. |
| R02 — Timeline reports 1600×1000 for an actual 480×300 JPEG. | **Fixed locally.** Timeline v2 records actual encoded pixels separately from viewport dimensions; receipts include output dimensions. | Independently decode frames and compare metadata/output, including mixed source sizes, DPR and native cursor mapping. Preserve this regression in package acceptance. |
| R03 — Interleaved clipped screenshots corrupt the screencast. | **Fixed locally.** Preview/recording share one stream. Ordinary viewport crops avoid compositor resizing; disruptive full-page/raw clips pause and restore full-view capture before resuming. Pausing alone failed and was rejected. | Installed real-browser tests must retain correct frame geometry through region/ref/full-page/raw screenshots, cancellation and target closure. Verify neither consumer stops the other's stream. |
| R04 — Retina detail, DPR=1 and a 1600px cap make icons/text soft; expose scale/fps. | **Partial.** `maxSide` and fps are explicit; pixel provenance is accurate. There is no general recording DPR/scale contract yet. | Define source density versus output cap, expose supported options and report limitations. Test text/icon readability at 1×/2× and small/background windows. Do not force viewport/DPR changes on a regular user tab merely to make the recording appear sharper. |
| R05 — Why cap capture at 30 instead of 60 fps? | **Browser fixed locally; native open.** Browser default/max is 60; timer drift and extra waits after writes are fixed. Dense fixture retained 315 distinct frames in 5.33 s, zero drops, ~59.8 preview updates/s. | Prove sustained actual frames and click-to-visible latency at the declared size on installed targets. Native Mac/X11 still request 30; GNOME polls at ~5. Thread fps through source capture/capabilities and replace slow backends, not merely encode duplicate frames at 60. |
| R06 — Codex icon is an indistinct blob inside the recording's cursor/ring. | **Partial.** The reporting agent enlarged the app glyph from 20px to 24px. That is a separate app UI edit, not evidence that Local Control cursor design is complete. | Inspect compressed output at ordinary viewing size for cursor/glyph legibility, click indication and coordinate accuracy. Distinguish an app's loading glyph from the agent cursor. Cover scrolling, drag and native gestures; semantic actions without pointer dispatch must not invent motion. |
| R07 — Preview feels laggy / native panel shows only a still. | **Fixed locally for polling/ownership and decode starvation; installed acceptance open.** A bounded viewer decoder replaces repeatedly cancelled async-image loads; compositor tests reach ~59–60 distinct fps from 1080p source pixels. Shared streams and hidden-consumer release remain. [Evidence](local-control-preview-evidence.md). | Repeat on installed shared-host/Aside and remote paths, full-size/high-DPR viewers, visibility/retargeting and sustained load. Local two-viewer + recording input/fidelity/cleanup passed. Native 60 fps constraints remain requests, not measured delivery. |
| R08 — Capture does unnecessary work and may slow control. | **Partial.** Bounded JPEGs bypass repeated decode/resize/re-encode; one pending frame bounds recording work; ACKs are independent of disk/UI. Text-only reads explicitly avoid screenshots. | Measure CPU, memory, bytes and input latency. Dense preview JSON can reach ~555 KB/frame and ~33 MB/s at 60 fps. Compare transport alternatives with unchanged text fidelity and no unwanted image work for semantic reads. |
| R09 — Background/no-frame recording can appear to start successfully. | **Fixed locally for truthful startup; coverage open.** Start waits for the first persisted frame and refuses after five seconds without one. A separate owned headless window paints in the tested route; inactive tabs can fail. | Clear recovery must preserve target/foreground and avoid silent activation, movement or replacement. Test regular-profile background, occluded, minimized and inactive cases independently; disclose unsupported capture modes before promising a recording. |
| R10 — Long runs, restart/cancellation and dependency failures can lose media. | **Partial.** Final-frame flush, retained interruption receipts, bounded finalization, bundled Mac encoders and ARM64/Intel cloud cleanup have evidence. | Exercise sustained size/duration limits, disk pressure, encoder failure, resize, crashes/reloads and abrupt teardown on packaged targets. Retain playable partial media where possible and explicit failure otherwise. Include matching action/cursor timelines; audio recording remains unsupported. |
| R11 — Host fps is being used where displayed smoothness and interaction latency matter. September streaming review. | **Partial: local compositor evidence added.** Independent pixel markers measure distinct frames, gaps and click/type/scroll-to-visible; exact source-state and full decoded-pixel checks pass. [Measured results and scope](local-control-preview-evidence.md). | Measure installed/remote and full-size viewers, stage costs, independent second-viewer timing and instrumentation overhead; local offscreen composition is not physical display or network latency. [Plan](local-control-streaming.md#1-measure-the-viewer-before-selecting-a-transport). |
| R12 — Full-frame processing and separate viewers may waste capture/encoding work. | **Partial.** Two production viewers plus recording start one capture; each closes independently, with identical decoded pixels. Negotiated lossless compression reduced shared-socket response bodies by 32.86%; Electron IPC still carries expanded preview JSON. A later CPU-instrumented run was slower, so sustained resource/tail acceptance remains open. Damage-aware processing, remote transport and compatible encoder sharing remain unproven. | Compare WebRTC/local candidates against JPEG baseline; measure idle/active CPU, bytes and one/two-viewer plus recording costs. Preserve text fidelity, source scope and independent recording quality. [Experiments](local-control-streaming.md#2-compare-transport-and-capture-changes-separately). |
| R13 — Quiet/hidden sources can be confused with a broken connection. | **Open acceptance gap, not a reproduced Mako reconnect bug.** No-first-frame refusal exists; distinct source/viewer/transport lifecycle evidence is incomplete. | Test idle, hide/show, source loss, sleep/wake and transport failure separately. Recover the failed component without disrupting healthy viewers/recordings or restoring stale input leases. [Recovery](local-control-streaming.md#4-recover-only-the-failed-component). |
| R14 — Audio delivery and audio recording lack an explicit capability/acceptance plan. | **Deferred capability expansion.** It is not required for current low-latency video work. Neither video capture nor owned-tab audio cleanup establishes audio streaming/recording. | Specify output source, mute/opt-in, A/V sync and cleanup separately for preview and files; do not implicitly capture microphone/unrelated-app audio. [Scope](local-control-streaming.md#audio-scope). |
| R15 — Native screenshot options silently ignored. Found during CLI acceptance. | **Fixed locally.** `maxSide:320` previously returned 640×420; shared native capture now returns 320×210, honors JPEG options and maps resized pixel input to the driver frame. Default bytes bypass reencoding. | Real GTK files/JPEG and shared-engine coordinate/stale-view checks pass. Preserve installed Mac/Linux tests; background X11 pointer input remains explicitly unsupported. [Evidence](audits/2026-09-23/local-control-cli-media.md). |


| R16 — Native fps was fixed and Linux capture stalled on pipe writes. | **Fixed in the +19 candidate; broader acceptance open.** Source fps now reaches ScreenCaptureKit/X11, and GNOME declares its real 5 fps ceiling. The Linux writer slept 5 ms every time its pipe filled; the 60 fps request retained only 33 source frames in 2.37 seconds. Writability polling raises the covered 640×420 source to 56.70 fps; encoded output fps is reported separately. | Measure retained source frames separately from CFR output, plus exact pixels, interruption cleanup and sustained 1080p CPU/memory. [Backend plan](local-control-capture-backends.md). |
| R17 — First-operation Mac recording crashes in ScreenCaptureKit. | **Reproduced.** The +18 candidate aborted in `SCContentFilter` / `SLSGetDisplaysWithRect`; a prior observation avoided that crash. +19 explicitly initializes the CoreGraphics display connection and checks window geometry before constructing the filter. First-operation capture passed at 1920×1080 with 577 distinct frames in 10.03 s and unchanged foreground samples. | Recording must work as the first operation with no extra agent observation, activation or capture-scope change. Retain first-use and repeated-start checks. |
| R18 — Sustained browser recording fills the source-JPEG storage cap. | **Reproduced; open.** The explicit 1080p, two-viewer, 60-second run reached 52.02 distinct fps. Recording retained 2,379 JPEGs (536,971,545 bytes), then stopped at 41.33 seconds. Streaming the later RGBA encoding step did not eliminate source-image accumulation. | Encode during capture with bounded buffering, source timestamps, accurate cursor composition and playable interrupted output. Keep the failed run; do not raise the cap to claim a pass. [Measurements](local-control-preview-evidence.md#september-24-sustained-1080p-failure). |


Owner: [LC-21 capture](local-control-map.md#lc-21--responsive-capture-recordings-and-cursor).
Packaging and final installed proof also depend on LC-26 and LC-23.

## Other reported blockers and inherited correctness work

| ID / report | Current disposition | Closure check / owner |
| --- | --- | --- |
| B01 — Full-tree lint flags `message: object` in the cloud worker. | **Fixed locally.** Final full lint passed with zero errors and five existing React/TanStack warnings. | Preserve full-tree checks; distinguish an unrelated concurrent failure from one introduced here. See the capture audit's `verification/` logs. |
| B02 — Raw-call uncertainty and broad invalidation from the earlier API review. | **Target recovery implemented and fault-tested locally.** Browser/raw native failures preserve unknown outcomes; exact-target reads recover, unrelated refs survive, driver reconnect and failed captures cannot clear uncertainty. Late monitoring failure, cancelled waits/programs, overlapping observations and waiting dialogs have regressions. | Browser-wide cookies, raw CDP restrictions, attach/detach ownership, in-flight profile locks and lost-reply recovery now pass local regressions in the [shared-engine audit](audits/2026-09-23/local-control-session-cli.md). Remaining: real process loss during input and installed replacement. [Recovery audit](audits/2026-09-23/local-control-recovery.md); [LC-08/14](local-control-map.md#lc-08--lc-14--uncertainty-ownership-and-independent-targets). |
| B03 — “Passed locally” repeatedly confused with “available in the installed app.” | **Current build needs acceptance.** The queued installer aborted on host replacement. Running build `3e3f6a31a7970952` contains preview compression; the earlier candidate’s checks do not certify it. Aside 0.3.2 passed 20 complete jobs; hidden-tab recording then refused for no frames. Earlier evidence remains separate. | Record exact host, extension and driver versions and rerun the relevant original workflow after safe installation. [LC-23](local-control-map.md#lc-23--preview-isolation-and-installed-browser-rollout). |

## How to close an issue

Attach the reproducer, changed code/contract and independent outcome check. State
which target/build was tested and whether deployment happened. Preserve rejected
explanations and failed candidates. Link the original complaint to its check;
“better help”, “60 fps output” or a passing unit test alone does not close an
end-to-end usability or fidelity complaint. Reopen an entry if a real agent still
needs a workaround that its closure check says should be unnecessary.


## September 24 acceptance findings

- **R09:** source-host Aside live capture now shares temporary focus emulation
  with input. Capture/screenshot/transport-loss restoration passes. CDP clicks
  can retain page `hasFocus()` while hidden; physical foreground and DOM focus
  are reported separately. No tab activation or forced blur fallback.
- **R10:** fixed the reproduced 512 MiB PNG staging failure with bounded encoder
  streaming. A 37.73-second two-viewer recording completes with the minimal
  bundled encoder. Static duration and encoder failure/reaping have regressions.
- **R08/R12:** 1080p two-viewer delivery reaches 58.03 distinct fps for 30 seconds,
  but expanded IPC remains 17.67 MB/s. The 60-second 1440p run reaches only
  34.00 fps. High-resolution efficiency remains open; preserve the slower run.
- **A05/A08/A10:** fresh agents pass installed CLI/MCP scoped editing, exact
  values, stdin/files and one-element capture. CLI `shot --format png|jpeg` now
  makes encoding explicit; filename extensions alone never selected it.
- **Native keyboard:** source high-level Return now completes the verified
  Terminal job. Explicitly off-screen siblings no longer over-restrict the
  admission hint. Recording's live AX dialog still trips the driver's ambiguity
  guard; title/size guesses do not remove it.

[Evidence and remaining acceptance](local-control-preview-evidence.md#september-24-capture-ownership-and-long-recording).
